{
  "name": "noynoynoy",
  "private": true,
  "version": "0.0.0",
  "description": "Static site built with Parcel for CI-only bundling",
  "scripts": {
    "build": "parcel build index.html --dist-dir dist --public-url ./",
    "serve": "parcel serve index.html"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "packageManager": "yarn@1.22.22"
}

